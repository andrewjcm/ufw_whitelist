# GitHub Actions UFW Whitelist Configuration

# TCP port to whitelist for GitHub Actions IPs
# This is the port that GitHub Actions runners will be allowed to access
# Example: 22 for SSH, 443 for HTTPS, or any custom port
TCP_PORT=22
